#!/bin/bash

echo "🏗️  Building Jekyll site..."
bundle exec jekyll build

echo "🚀 Starting local Heroku server..."
export PORT=5000
export JEKYLL_ENV=production
export RACK_ENV=production

bundle exec rackup config.ru -p $PORT 